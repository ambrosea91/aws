# ============================================================================
# AWS CDK Infrastructure - Environment Variables Template
# ============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. Load variables: export $(cat .env | xargs)
#
# ============================================================================

# ----------------------------------------------------------------------------
# AWS Configuration
# ----------------------------------------------------------------------------
AWS_ACCOUNT_ID=123456789012
AWS_DEFAULT_REGION=us-east-2
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# ----------------------------------------------------------------------------
# Region Configuration (Global Databases)
# ----------------------------------------------------------------------------
# Primary Region: us-east-2 (Ohio) - Read/Write
PRIMARY_REGION=us-east-2

# Secondary Region: us-west-2 (Oregon) - Read-Only (Disaster Recovery)
SECONDARY_REGION=us-west-2

# ----------------------------------------------------------------------------
# EC2 Configuration
# ----------------------------------------------------------------------------
# EC2 Key Pair Name (must exist in AWS before deployment)
# Create with: aws ec2 create-key-pair --key-name my-keypair --region us-east-2
EC2_KEY_PAIR_NAME=my-cdk-keypair

# ----------------------------------------------------------------------------
# GitHub Configuration (for Jenkins CI/CD)
# ----------------------------------------------------------------------------
# GitHub Personal Access Token with 'repo' scope
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub Repository (optional)
GITHUB_REPO=your-org/cdk-infrastructure

# ----------------------------------------------------------------------------
# Jenkins Server Configuration (Deployed by CDK)
# ----------------------------------------------------------------------------
# Jenkins will be deployed to: http://<jenkins-public-ip>:8080
# After deployment, get URL from CloudFormation outputs

# Jenkins EC2 instance type
JENKINS_INSTANCE_TYPE=t3.medium

# Jenkins VPC CIDR
JENKINS_VPC_CIDR=10.2.0.0/16

# Allowed IP addresses for Jenkins access (comma-separated, CIDR notation)
# IMPORTANT: Restrict to your IP for security
# Get your IP: curl -s ifconfig.me
JENKINS_ALLOWED_IPS=1.2.3.4/32,5.6.7.8/32

# ----------------------------------------------------------------------------
# SonarQube Server Configuration (Deployed by CDK)
# ----------------------------------------------------------------------------
# SonarQube will be deployed to: http://<sonarqube-public-ip>:9000
# After deployment, get URL from CloudFormation outputs
# Default credentials: admin / admin (change on first login)

# SonarQube EC2 instance type (recommended: t3.large for better performance)
SONARQUBE_INSTANCE_TYPE=t3.large

# SonarQube database instance type
SONARQUBE_DB_INSTANCE_TYPE=db.t3.small

# SonarQube VPC CIDR
SONARQUBE_VPC_CIDR=10.3.0.0/16

# Allowed IP addresses for SonarQube access (comma-separated, CIDR notation)
# IMPORTANT: Restrict to your IP for security
SONARQUBE_ALLOWED_IPS=1.2.3.4/32,5.6.7.8/32

# SonarQube authentication token (generate after deployment)
# Generate at: SonarQube UI > My Account > Security > Generate Tokens
SONAR_TOKEN=squ_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# SonarQube project key prefix
SONAR_PROJECT_KEY=cdk-infrastructure

# ----------------------------------------------------------------------------
# CDK Configuration (optional overrides)
# ----------------------------------------------------------------------------
# CDK_DEFAULT_ACCOUNT is usually read from AWS credentials
# CDK_DEFAULT_REGION is usually read from AWS_DEFAULT_REGION
# Uncomment to override:
# CDK_DEFAULT_ACCOUNT=123456789012
# CDK_DEFAULT_REGION=us-east-2

# ----------------------------------------------------------------------------
# Database Configuration (optional)
# ----------------------------------------------------------------------------
# Uncomment to customize database settings (defaults are in the stack code)
# DB_INSTANCE_TYPE=t3.medium
# DB_BACKUP_RETENTION_DAYS=7
# DB_ENABLE_DELETION_PROTECTION=true

# ----------------------------------------------------------------------------
# Cost Allocation Tags (optional)
# ----------------------------------------------------------------------------
# PROJECT=CDK-Infrastructure
# ENVIRONMENT=Production
# COST_CENTER=Engineering
# OWNER=DevOps-Team
